# Django-Authentication-System
A simple Django web application implementing user authentication with registration, login, logout, and a protected dashboard using Django's built-in authentication system and SQLite database.
